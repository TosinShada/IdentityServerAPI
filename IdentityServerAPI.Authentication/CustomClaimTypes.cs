namespace IdentityServerApi.Authentication;

public class CustomClaimTypes
{
    public const string ApplicationId = nameof(ApplicationId);
    public const string Age = nameof(Age);
    public const string Amr = "amr";
}
